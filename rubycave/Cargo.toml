[package]
name = "rubycave"
version = "0.1.0"
edition = "2021"

[dependencies]
glam = { version = "0.28.0", features = ["debug-glam-assert", "bytemuck"] }
rkyv = { version = "0.7.44", features = ["archive_le", "std", "validation"] }

[build-dependencies]
glob = "0.3.1"
